# Modelos preentrenados

El modelo base debe copiarse en `07_models/pretrained/t5-small/`.

La carpeta debe contener directamente los archivos de Hugging Face, incluyendo
`config.json`, los pesos del modelo y los archivos del tokenizer.

La ruta puede cambiarse al ejecutar el entrenamiento mediante `--model-path`.
